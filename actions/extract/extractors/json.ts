import * as core from "@actions/core";

export default async function jsonExtractor(settings: {
  workingDirectory: string;
  dataDirectory: string;
}) {
  core.warning("jsonExtractor not implemented");
}
